import { QuestionOptionsDirective } from './question-options.directive';

describe('QuestionOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new QuestionOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
